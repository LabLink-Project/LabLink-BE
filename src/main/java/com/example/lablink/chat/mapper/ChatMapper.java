package com.example.lablink.chat.mapper;

import com.example.lablink.chat.dto.ChatMessageDTO;
import com.example.lablink.chat.entity.ChatMessage;
import com.example.lablink.chat.entity.ChatRoom;
import com.example.lablink.user.dto.response.UserDto;
import com.example.lablink.user.entity.User;
import org.mapstruct.Mapper;

import java.util.List;

@Mapper(componentModel = "spring")
public interface ChatMapper {
//    User ChatPostDtoToMember(ChatDto.Post post);
//    UserDto.ResponseOnlyUserName memberToMembernameResponseDto(User user);
//
//    ChatDto.RoomResponse chatRoomToRoomResponseDto(ChatRoom chatRoom);
//    List<ChatDto.RoomResponse> chatRoomListToRoomResponseDtos(List<ChatRoom> chatRooms);
//
//    ChatDto.MessageResponse messageToMessageResponseDto(ChatMessage message);
//
//    List<ChatMessageDTO> messagesToMessageResponseDtos(List<ChatMessage> messages);

}
